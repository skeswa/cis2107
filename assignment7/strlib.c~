#include <stdlib.h>

int strlen(char *s) {
	int i = 0;
	if (is_blank(s)) return 0;
	while (s* != 0) {
		i++;
		s++;
	}
	return i;
}

int is_blank(char *s) {
	if (!s) return 1;
	if (!*s) return 1;
	return 0;
}
	
